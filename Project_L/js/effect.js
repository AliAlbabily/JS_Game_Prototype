
// Every element in "elements" requires a selector in "selectors"

// First room effect
const shadowing = {
  container: "body",
  elements: ['<div id="shadowing" class="catch"></div>'],
  selectors: ['#shadowing']
};
